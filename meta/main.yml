galaxy_info:
  author: openstax
  description: ansible-role-papertrail
  company: openstax
  license: GPLv2
  min_ansible_version: 2.1
  github_branch: master
  platforms:
    - name: Ubuntu
      versions:
        - trusty
        - xenial
    - name: Debian
      versions:
        - all
  
  galaxy_tags: [openstax, ansible-role-papertrail, ]

dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
